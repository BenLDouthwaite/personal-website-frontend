import React, { Component } from "react";

class Blog extends Component {
  render() {
    return (
      <div>
        <h2>Blog</h2>
        <p>I should probably write some blog posts too...</p>

        <p> TODO Add info about how to handle nested routes. E.g. for blog posts and also for projects</p>
        <p>Frustrations with dynamic loading on my env. specifically default returning undefined.</p>
      </div>
    );
  }
}

export default Blog;