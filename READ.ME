# Website front end. needs a good old tidy up when I have time

Test commit to the read me to check if submodules work

## Build and deploy

To deploy locally, run `npm start`

This will trigger a rebuild of the webpack package for local and serve the pages.

Can optionally run `npm run watch` to watch files for changes.

TODO Look into if I can update the monitoring command to watch and update the screen automatically
TODO cont. as with the create-react-app package.